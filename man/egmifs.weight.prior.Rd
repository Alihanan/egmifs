% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weight_prior.R
\name{egmifs.weight.prior}
\alias{egmifs.weight.prior}
\title{Construct prior-informed penalty weights}
\usage{
egmifs.weight.prior(
  prior,
  eta = 1,
  score = NULL,
  reference = NULL,
  label = deparse(substitute(prior))
)
}
\arguments{
\item{prior}{Logical, non-negative numeric, factor, or character vector with
one entry per penalized predictor.}

\item{eta}{One or more positive prior-strength values. \code{eta = 1} gives equal
raw weights. For a binary prior, supported predictors receive raw weight
\code{1 / eta}.}

\item{score}{Optional score specification. For categorical priors, use a
named numeric vector such as \code{c(none = 0, predicted = 1, validated = 2)}.
An unnamed numeric vector of length \code{prior} is also accepted.}

\item{reference}{Optional reference category for an unscored categorical
prior. It receives score 0 and all other categories receive score 1.}

\item{label}{Optional short label describing the prior source.}
}
\value{
An object of class \code{egmifs_weight_prior}.
}
\description{
Creates one or more predictor-weight vectors from binary, ordinal, or
categorical prior information. For prior score \code{s[j]} and strength \code{eta},
the raw penalty weight is \code{eta^(-s[j])}. The main \code{\link[=egmifs]{egmifs()}} wrapper
performs its usual mean-one normalization separately for every fitted model.
}
\details{
Logical prior values are converted to scores 0 and 1. Non-negative numeric
prior values are used directly as scores, which supports ordinal evidence
levels. Character or factor priors require either a named \code{score} mapping or
a \code{reference} category; with \code{reference}, the reference receives score 0 and
every other category receives score 1.
}
