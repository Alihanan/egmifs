% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameters.R
\name{parameters}
\alias{parameters}
\alias{params}
\alias{parameters.egmifs}
\alias{parameters.egmifs_multi_alpha}
\alias{parameters.egmifs_tuning_grid}
\title{Extract all fitted model parameters}
\usage{
parameters(object, ...)

params(object, ...)

\method{parameters}{egmifs}(
  object,
  criterion = NULL,
  iteration = NULL,
  state = NULL,
  drop = TRUE,
  ...
)

\method{parameters}{egmifs_multi_alpha}(
  object,
  criterion = NULL,
  iteration = NULL,
  alpha = NULL,
  state = NULL,
  drop = TRUE,
  ...
)

\method{parameters}{egmifs_tuning_grid}(
  object,
  criterion = NULL,
  iteration = NULL,
  alpha = NULL,
  eta = NULL,
  state = NULL,
  drop = TRUE,
  ...
)
}
\arguments{
\item{object}{An egmifs fit, multi-alpha fit, or alpha by prior-strength
tuning grid.}

\item{...}{Arguments passed to the corresponding class method.}

\item{criterion}{Optional criterion name or names selecting their best stored
states.}

\item{iteration}{Optional numeric iteration or iterations. The nearest saved
state is used for each value.}

\item{state}{Optional explicit state selector: \code{"path"} or \code{"terminal"}.
It cannot be combined with \code{criterion} or \code{iteration}.}

\item{drop}{Logical. For one selected state, return vectors instead of
one-row matrices.}

\item{alpha}{Optional stored alpha value or values, or their labels.}

\item{eta}{Optional stored prior-strength value or values, or their labels.}
}
\value{
A named list with components \code{beta}, \code{theta}, \code{family}, and \code{link}.
Each component is a matrix for a path or multiple states, and a named
numeric vector for one selected state when \code{drop = TRUE}.
}
\description{
\code{parameters()} extracts penalized coefficients, unpenalized coefficients,
family parameters, and link parameters from saved path states. \code{params()} is
a shorter alias with identical behavior.
}
\details{
With no selector, the saved parameter path is returned. Use
\code{state = "terminal"}, one or more criterion names, or one or more iterations
to select particular fitted states.
}
